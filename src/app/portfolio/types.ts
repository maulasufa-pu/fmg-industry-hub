export const WORK_TYPES = ["arrangement", "production", "mixing", "mastering", "songwriting", "publishing", "release"] as const;
export type WorkType = typeof WORK_TYPES[number];
export type PortfolioItem = { id:number; genre:string; song_title:string; album_title:string|null; singer:string[]; arranger:string[]; producer:string[]; mixing_engineer:string[]; mastering_engineer:string[]; songwriter:string[]; composer:string[]; publisher:string[]; aggregator:string[]; release_date_aggregator:string|null; spotify_link:string|null; youtube_link:string|null; apple_music_link:string|null; artwork_link:string|null; priority_order:number|null; is_featured:boolean|null; work_type:WorkType[]; client_brief:string|null; challenge:string|null; arrangement_solution:string|null; before_url:string|null; after_url:string|null; turnaround_days:number|null; revision_count:number|null; deliverables:string[]; testimonial_quote:string|null; testimonial_name:string|null; };
export type PortfolioResponse = { data:PortfolioItem[]; pagination:{page:number;limit:number;total:number;totalPages:number}; facets:{genres:string[]} };
