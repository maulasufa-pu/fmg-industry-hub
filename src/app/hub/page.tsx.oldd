"use client";

import React, { useMemo } from "react";
import Link from "next/link";
import { motion, Variants, useScroll, useTransform } from "framer-motion";
import { ArrowRight, PlayCircle, Sparkles, Music, LineChart, ShieldCheck } from "lucide-react";

/*****************************************
 * Types
 *****************************************/

type Service = {
  label: string;
  description: string;
  hrefPrimary: string;
  hrefSecondary?: string;
  icon: React.ComponentType<React.SVGProps<SVGSVGElement>>;
  highlights?: readonly string[];
};

type Post = {
  title: string;
  href: string;
  tag: "Blog" | "News";
  dateISO: string; // YYYY-MM-DD
};

/*****************************************
 * Animation Variants (match landing vibe)
 *****************************************/

const fadeUp: Variants = {
  hidden: { opacity: 0, y: 24 },
  visible: (i: number = 0) => ({
    opacity: 1,
    y: 0,
    transition: { duration: 0.6, ease: "easeOut", delay: i * 0.06 },
  }),
};

const scaleIn: Variants = {
  hidden: { opacity: 0, scale: 0.98 },
  visible: { opacity: 1, scale: 1, transition: { duration: 0.5, ease: "easeOut" } },
};

/*****************************************
 * Data (replace with CMS as needed)
 *****************************************/

const services: readonly Service[] = [
  {
    label: "Flemmo Studio",
    description: "Recording • Mixing • Mastering — release‑ready sound with two‑stage QC.",
    hrefPrimary: "/studio/booking",
    hrefSecondary: "/studio/services",
    icon: Music,
    highlights: ["Hybrid analog/digital", "A‑list engineering workflow", "QC 2‑step"],
  },
  {
    label: "Flemmo Creative",
    description: "MV • Content • Campaign — visuals and narratives that actually ship.",
    hrefPrimary: "/creative/brief",
    hrefSecondary: "/creative/portfolio",
    icon: Sparkles,
    highlights: ["Full‑stack production", "Campaign & rollout", "Brand safety"],
  },
  {
    label: "Flemmo Academy",
    description: "Education & Masterclasses — industry‑grade learning, real workflows.",
    hrefPrimary: "/academy/courses",
    hrefSecondary: "/academy/enroll",
    icon: PlayCircle,
    highlights: ["Mentor‑led", "Project‑based", "Career‑ready"],
  },
  {
    label: "FMG Publishing",
    description: "Distribution & Publishing — transparent splits, clear rights, clean reporting.",
    hrefPrimary: "/publishing/start-release",
    hrefSecondary: "/publishing/how-it-works",
    icon: ShieldCheck,
    highlights: ["Royalty splits", "Metadata & QC", "DSP pitch"],
  },
] as const;

const samplePosts: readonly Post[] = [
  { title: "How we cut idea→release time by 30%", href: "/blog/idea-to-release-30", tag: "Blog", dateISO: "2025-08-01" },
  { title: "FMG Publishing: 3 years serving creators", href: "/newsroom/fmg-publishing-3-years", tag: "News", dateISO: "2025-07-10" },
] as const;

/*****************************************
 * Utilities
 *****************************************/

function cx(...classes: Array<string | false | null | undefined>): string {
  return classes.filter(Boolean).join(" ");
}

function formatDate(iso: string): string {
  const d = new Date(iso);
  return d.toLocaleDateString(undefined, { year: "numeric", month: "short", day: "numeric" });
}

/*****************************************
 * Magnetic Button (from landing theme)
 *****************************************/
function MagneticButton({ children, href, className = "" }: { children: React.ReactNode; href?: string; className?: string }) {
  const Btn = (
    <motion.button
      whileHover={{ x: 1, y: -1 }}
      whileTap={{ scale: 0.98 }}
      className={cx(
        "group relative inline-flex items-center gap-2 rounded-2xl px-5 py-3 text-sm font-semibold",
        "bg-black text-white dark:bg-white dark:text-black",
        "shadow-[0_8px_30px_rgb(0,0,0,0.12)] transition-colors",
        "hover:bg-gradient-to-r hover:from-indigo-600 hover:to-violet-600 hover:text-white",
        className
      )}
    >
      {children}
      <ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-0.5" />
      <span className="pointer-events-none absolute inset-0 rounded-2xl bg-white/10 opacity-0 blur-xl transition-opacity group-hover:opacity-100 dark:bg-black/10" />
    </motion.button>
  );
  if (href) return (
    <Link href={href} className="inline-block">{Btn}</Link>
  );
  return Btn;
}

/*****************************************
 * Page Component — THEME UPDATED
 *****************************************/
export default function HomePage(): React.JSX.Element {
  const { scrollYProgress } = useScroll();
  const scaleHero = useTransform(scrollYProgress, [0, 0.2], [1, 0.985]);

  const serviceCols = useMemo(() => services, []);

  return (
    <main className="relative min-h-screen bg-white text-black antialiased dark:bg-black dark:text-white selection:bg-black/10 selection:text-black dark:selection:bg-white/20 dark:selection:text-white">
      {/* subtle noise overlay */}
      <div className="pointer-events-none fixed inset-0 z-[-1] opacity-[0.06] mix-blend-soft-light" aria-hidden>
        <svg className="h-full w-full" xmlns="http://www.w3.org/2000/svg">
          <filter id="noiseFilter">
            <feTurbulence type="fractalNoise" baseFrequency="0.80" numOctaves="4" stitchTiles="stitch" />
            <feColorMatrix type="saturate" values="0" />
          </filter>
          <rect width="100%" height="100%" filter="url(#noiseFilter)" />
        </svg>
      </div>

      {/* Ambient radial like landing */}
      <div aria-hidden className="pointer-events-none fixed inset-0 -z-10">
        <div className="absolute inset-0 bg-[radial-gradient(1200px_500px_at_50%_-120px,rgba(79,70,229,0.15),transparent)]" />
      </div>

      {/* Header (light) */}
      <header className="sticky top-0 z-50 border-b border-black/10 bg-white/70 backdrop-blur supports-[backdrop-filter]:bg-white/60 dark:border-white/10 dark:bg-black/40">
        <div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
          <Link href="/" className="flex items-center gap-2 font-semibold">
            <div className="h-6 w-6 rounded-md bg-gradient-to-br from-indigo-600 to-fuchsia-600" />
            <span>Flemmo Music Global</span>
          </Link>
          <nav className="hidden md:flex items-center gap-6 text-sm text-black/80 dark:text-white/80">
            <Link className="hover:text-black dark:hover:text-white" href="/studio">Studio</Link>
            <Link className="hover:text-black dark:hover:text-white" href="/creative">Creative</Link>
            <Link className="hover:text-black dark:hover:text-white" href="/academy">Academy</Link>
            <Link className="font-medium hover:text-black dark:hover:text-white" href="/publishing">Publishing</Link>
            <Link className="hover:text-black dark:hover:text-white" href="https://hub.flemmomusic.com">FMGIHub</Link>
          </nav>
          <div className="hidden sm:flex items-center gap-3">
            <MagneticButton href="/publishing/start-release">Start a Release</MagneticButton>
            <Link href="/studio/booking" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-5 py-3 text-sm font-semibold shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:hover:bg-black">
              Book the Studio
            </Link>
          </div>
        </div>
      </header>

      {/* Hero */}
      <section className="relative">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-18 sm:py-24">
          <motion.div style={{ scale: scaleHero }} initial="hidden" animate="visible" variants={fadeUp}>
            <div className="mb-3 inline-flex items-center gap-2 rounded-full border border-black/10 bg-white/70 px-3 py-1 text-xs shadow-sm backdrop-blur dark:border-white/10 dark:bg-black/40">
              <Sparkles className="h-4 w-4 text-indigo-600" />
              <span>Integrated creation → release workflow</span>
            </div>
            <h1 className="text-4xl sm:text-5xl md:text-6xl font-bold leading-tight tracking-tight">
              Artist‑First Infrastructure.
            </h1>
            <p className="mt-5 max-w-2xl text-lg sm:text-xl text-black/70 dark:text-white/70">
              Produksi studio, konten, edukasi, serta distribusi &amp; publishing — terintegrasi lewat <span className="text-black dark:text-white font-semibold">FMGIHub</span>. Gerak dari ide sampai rilis tanpa friksi.
            </p>
            <div className="mt-8 flex flex-wrap gap-3">
              <MagneticButton href="/publishing/start-release">Start a Release</MagneticButton>
              <Link href="/studio/booking" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-5 py-3 text-sm font-semibold shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:hover:bg-black">
                Book the Studio
              </Link>
              <Link href="https://hub.flemmomusic.com/network" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/60 px-5 py-3 text-sm font-semibold text-black/80 transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:text-white/80 dark:hover:bg-black">
                Join the Network
              </Link>
            </div>

            {/* Proof bar */}
            <div className="mt-10 grid grid-cols-1 gap-4 sm:grid-cols-3">
              {[
                "FMG Publishing: 3+ years",
                "Founder: Sony Music Group Global Scholars",
                "Global collaborations",
              ].map((item, i) => (
                <motion.div
                  key={item}
                  custom={i}
                  initial="hidden"
                  whileInView="visible"
                  viewport={{ once: true, amount: 0.6 }}
                  variants={fadeUp}
                  className="rounded-xl border border-black/10 bg-white/70 px-4 py-3 text-sm text-black/70 shadow-sm backdrop-blur dark:border-white/10 dark:bg-black/40 dark:text-white/70"
                >
                  {item}
                </motion.div>
              ))}
            </div>
          </motion.div>
        </div>
      </section>

      {/* Services */}
      <section aria-labelledby="services" className="relative py-14 sm:py-18">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="flex items-end justify-between gap-6">
            <h2 id="services" className="text-2xl sm:text-3xl font-bold tracking-tight">What We Do</h2>
            <Link href="/about" className="text-black/60 hover:text-black dark:text-white/60 dark:hover:text-white text-sm">About Flemmo</Link>
          </div>
          <div className="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">
            {serviceCols.map((s, i) => (
              <motion.article
                key={s.label}
                className="group relative rounded-2xl border border-black/10 bg-white p-5 shadow-sm transition hover:shadow-xl dark:border-white/10 dark:bg-black"
                initial="hidden"
                whileInView="visible"
                viewport={{ once: true, amount: 0.5 }}
                custom={i}
                variants={scaleIn}
              >
                <div className="flex items-center gap-3">
                  <s.icon className="h-5 w-5 text-indigo-600 dark:text-indigo-400" />
                  <h3 className="text-lg font-semibold">{s.label}</h3>
                </div>
                <p className="mt-3 text-sm text-black/70 dark:text-white/70 min-h-[56px]">{s.description}</p>
                {s.highlights && (
                  <ul className="mt-3 space-y-1 text-xs text-black/70 dark:text-white/70">
                    {s.highlights.map((h) => (
                      <li key={h} className="flex items-center gap-2">
                        <span className="inline-block h-1.5 w-1.5 rounded-full bg-indigo-500" /> {h}
                      </li>
                    ))}
                  </ul>
                )}
                <div className="mt-5 flex flex-wrap gap-2">
                  <MagneticButton href={s.hrefPrimary}>
                    {s.label.includes("Publishing") ? "Start a Release" : s.label.includes("Creative") ? "Submit Brief" : s.label.includes("Academy") ? "View Courses" : "Book Now"}
                  </MagneticButton>
                  {s.hrefSecondary && (
                    <Link href={s.hrefSecondary} className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-3 py-2 text-xs font-semibold text-black/80 shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:text-white/80 dark:hover:bg-black">
                      Learn more
                    </Link>
                  )}
                </div>
              </motion.article>
            ))}
          </div>
        </div>
      </section>

      {/* How it works */}
      <section aria-labelledby="how" className="relative py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="flex items-end justify-between gap-6">
            <h2 id="how" className="text-2xl sm:text-3xl font-bold tracking-tight">How It Works</h2>
            <Link href="https://hub.flemmomusic.com" className="text-black/60 hover:text-black dark:text-white/60 dark:hover:text-white text-sm">FMGIHub</Link>
          </div>
          <div className="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-5">
            {[
              { k: "Join", d: "Onboarding & verified profile." },
              { k: "Create", d: "Studio & Creative production pipeline." },
              { k: "Publish", d: "Metadata, QC, release via FMG Publishing." },
              { k: "Connect", d: "FMGIHub Network for briefs & collabs." },
              { k: "Grow", d: "Analytics & insights for next releases." },
            ].map((step, i) => (
              <motion.div key={step.k} custom={i} initial="hidden" whileInView="visible" viewport={{ once: true }} variants={fadeUp} className="rounded-2xl border border-black/10 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-black">
                <div className="text-sm uppercase tracking-wide text-black/60 dark:text-white/60">{String(i + 1).padStart(2, "0")}</div>
                <div className="mt-2 text-lg font-semibold">{step.k}</div>
                <p className="mt-1 text-sm text-black/70 dark:text-white/70">{step.d}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Featured work */}
      <section aria-labelledby="work" className="relative py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="flex items-end justify-between gap-6">
            <h2 id="work" className="text-2xl sm:text-3xl font-bold tracking-tight">Featured Work</h2>
            <div className="flex items-center gap-4 text-sm">
              <Link className="text-black/60 hover:text-black dark:text-white/60 dark:hover:text-white" href="/studio/portfolio">Studio Portfolio</Link>
              <Link className="text-black/60 hover:text-black dark:text-white/60 dark:hover:text-white" href="/creative/portfolio">Creative Portfolio</Link>
            </div>
          </div>
          <div className="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
            {[1, 2, 3, 4, 5, 6].map((i) => (
              <motion.figure key={i} initial="hidden" whileInView="visible" viewport={{ once: true }} variants={scaleIn} className="overflow-hidden rounded-2xl border border-black/10 bg-white">
                <div className="aspect-video bg-[radial-gradient(600px_300px_at_50%_0,rgba(99,102,241,0.15),transparent)]" />
                <figcaption className="p-4">
                  <div className="text-sm text-black/60 dark:text-white/60">Project {i}</div>
                  <div className="mt-1 text-base">Role: Production • Mix • Master</div>
                </figcaption>
              </motion.figure>
            ))}
          </div>
        </div>
      </section>

      {/* Publishing highlight */}
      <section className="relative py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-1 gap-10 lg:grid-cols-2 lg:items-center">
            <motion.div initial="hidden" whileInView="visible" viewport={{ once: true }} variants={fadeUp}>
              <h2 className="text-2xl sm:text-3xl font-bold tracking-tight">Distribution & Publishing that’s actually clear</h2>
              <p className="mt-3 text-black/70 dark:text-white/70">Royalty splits yang jelas, pelaporan yang kebaca, metadata &amp; QC rapi, dan pitch ke DSP. Bergerak dari demo ke rilis dengan kontrol penuh atas hak.</p>
              <div className="mt-6 flex flex-wrap gap-3">
                <MagneticButton href="/publishing/start-release">Start a Release</MagneticButton>
                <Link href="/publishing/how-it-works" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-5 py-3 text-sm font-semibold shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:hover:bg-black">
                  How it Works
                </Link>
                <Link href="/publishing/pricing" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-5 py-3 text-sm font-semibold shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:hover:bg-black">
                  See Pricing
                </Link>
              </div>
            </motion.div>
            <motion.div initial="hidden" whileInView="visible" viewport={{ once: true }} variants={scaleIn} className="rounded-2xl border border-black/10 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-black">
              <div className="flex items-center gap-3 text-black/80 dark:text-white/80"><LineChart className="h-5 w-5 text-indigo-600" /> Reporting Snapshot</div>
              <div className="mt-4 h-40 rounded-lg bg-[radial-gradient(600px_300px_at_50%_0,rgba(99,102,241,0.15),transparent)]" />
              <ul className="mt-4 grid grid-cols-2 gap-3 text-sm text-black/80 dark:text-white/80">
                <li className="rounded-lg border border-black/10 bg-white p-3 dark:border-white/10 dark:bg-black">T+3 payout option</li>
                <li className="rounded-lg border border-black/10 bg-white p-3 dark:border-white/10 dark:bg-black">Split by track & version</li>
                <li className="rounded-lg border border-black/10 bg-white p-3 dark:border-white/10 dark:bg-black">DSP pitch helper</li>
                <li className="rounded-lg border border-black/10 bg-white p-3 dark:border-white/10 dark:bg-black">Rights audit trail</li>
              </ul>
            </motion.div>
          </div>
        </div>
      </section>

      {/* Academy */}
      <section className="relative py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-1 gap-10 lg:grid-cols-3">
            <motion.div initial="hidden" whileInView="visible" viewport={{ once: true }} variants={fadeUp} className="lg:col-span-1">
              <h2 className="text-2xl sm:text-3xl font-bold tracking-tight">Flemmo Academy</h2>
              <p className="mt-3 text-black/70 dark:text-white/70">Kurikulum praktis yang bersumber dari workflow produksi dan rilis nyata. Mentor, project‑based, career‑ready.</p>
              <div className="mt-6 flex flex-wrap gap-3">
                <MagneticButton href="/academy/courses">View Courses</MagneticButton>
                <Link href="/academy/enroll" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-5 py-3 text-sm font-semibold shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:hover:bg-black">
                  Enroll Now
                </Link>
              </div>
            </motion.div>
            <motion.div initial="hidden" whileInView="visible" viewport={{ once: true }} variants={scaleIn} className="lg:col-span-2 grid grid-cols-1 gap-6 sm:grid-cols-2">
              {["Vocal Recording Modern", "Mixing for Streaming", "Release Strategy 101", "Sound Design for Visuals"].map((c) => (
                <article key={c} className="rounded-2xl border border-black/10 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-black">
                  <div className="aspect-video rounded-lg bg-[radial-gradient(600px_300px_at_50%_0,rgba(99,102,241,0.15),transparent)]" />
                  <h3 className="mt-3 text-base font-semibold">{c}</h3>
                  <p className="mt-1 text-sm text-black/70 dark:text-white/70">Mentor‑led • Project‑based</p>
                </article>
              ))}
            </motion.div>
          </div>
        </div>
      </section>

      {/* Logos / social proof */}
      <section aria-label="Social proof" className="relative py-10">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-2 gap-6 sm:grid-cols-4">
            {["Partner A", "Partner B", "Editor C", "Brand D"].map((n) => (
              <div key={n} className="h-10 rounded border border-black/10 bg-white text-center text-black/50 text-sm leading-10 dark:border-white/10 dark:bg-black dark:text-white/50">{n}</div>
            ))}
          </div>
        </div>
      </section>

      {/* Blog & News */}
      <section aria-labelledby="updates" className="relative py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="flex items-end justify-between gap-6">
            <h2 id="updates" className="text-2xl sm:text-3xl font-bold tracking-tight">From the Team</h2>
            <div className="flex items-center gap-4 text-sm">
              <Link className="text-black/60 hover:text-black dark:text-white/60 dark:hover:text-white" href="/blog">Blog</Link>
              <Link className="text-black/60 hover:text-black dark:text-white/60 dark:hover:text-white" href="/newsroom">Newsroom</Link>
            </div>
          </div>
          <div className="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2">
            {samplePosts.map((p, i) => (
              <motion.article key={p.href} custom={i} initial="hidden" whileInView="visible" viewport={{ once: true }} variants={fadeUp} className="rounded-2xl border border-black/10 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-black">
                <div className="text-xs text-black/60 dark:text-white/60">{p.tag} • {formatDate(p.dateISO)}</div>
                <h3 className="mt-2 text-lg font-semibold">
                  <Link href={p.href} className="hover:underline decoration-black/30 underline-offset-4 dark:decoration-white/40">{p.title}</Link>
                </h3>
              </motion.article>
            ))}
          </div>
        </div>
      </section>

      {/* Final CTA */}
      <section className="relative py-16 sm:py-24">
        <div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 text-center">
          <h2 className="text-3xl sm:text-4xl font-bold tracking-tight">Siap bawa proyekmu ke rilis?</h2>
          <p className="mt-4 text-black/70 dark:text-white/70 max-w-xl mx-auto">Mulai dari mana pun kamu berada—kami bantu bergerak dari ide ke rilis dengan kontrol atas hak, data, dan kualitas.</p>
          <div className="mt-8 flex flex-wrap items-center justify-center gap-3">
            <MagneticButton href="/publishing/start-release">Start a Release</MagneticButton>
            <Link href="/creative/brief" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-5 py-3 text-sm font-semibold shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:hover:bg-black">
              Submit a Brief
            </Link>
            <Link href="/studio/booking" className="inline-flex items-center gap-2 rounded-2xl border border-black/10 bg-white/70 px-5 py-3 text-sm font-semibold shadow-sm backdrop-blur transition hover:bg-white dark:border-white/10 dark:bg-black/40 dark:hover:bg-black">
              Book the Studio
            </Link>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="border-t border-black/10">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-12">
          <div className="grid grid-cols-2 gap-8 sm:grid-cols-4 text-sm">
            <div>
              <div className="font-semibold">Flemmo</div>
              <ul className="mt-3 space-y-2 text-black/70 dark:text-white/70">
                <li><Link href="/about" className="hover:text-black dark:hover:text-white">About</Link></li>
                <li><Link href="/careers" className="hover:text-black dark:hover:text-white">Careers</Link></li>
                <li><Link href="/contact" className="hover:text-black dark:hover:text-white">Contact</Link></li>
              </ul>
            </div>
            <div>
              <div className="font-semibold">Services</div>
              <ul className="mt-3 space-y-2 text-black/70 dark:text-white/70">
                <li><Link href="/studio" className="hover:text-black dark:hover:text-white">Studio</Link></li>
                <li><Link href="/creative" className="hover:text-black dark:hover:text-white">Creative</Link></li>
                <li><Link href="/academy" className="hover:text-black dark:hover:text-white">Academy</Link></li>
                <li><Link href="/publishing" className="hover:text-black dark:hover:text-white">Publishing</Link></li>
              </ul>
            </div>
            <div>
              <div className="font-semibold">FMGIHub</div>
              <ul className="mt-3 space-y-2 text-black/70 dark:text-white/70">
                <li><Link href="https://hub.flemmomusic.com" className="hover:text-black dark:hover:text-white">Overview</Link></li>
                <li><Link href="https://hub.flemmomusic.com/network" className="hover:text-black dark:hover:text-white">Network</Link></li>
                <li><Link href="https://hub.flemmomusic.com/products" className="hover:text-black dark:hover:text-white">Products</Link></li>
                <li><Link href="https://app.flemmomusic.com/login" className="hover:text-black dark:hover:text-white">Sign in</Link></li>
              </ul>
            </div>
            <div>
              <div className="font-semibold">Legal</div>
              <ul className="mt-3 space-y-2 text-black/70 dark:text-white/70">
                <li><Link href="/legal/terms" className="hover:text-black dark:hover:text-white">Terms</Link></li>
                <li><Link href="/legal/privacy" className="hover:text-black dark:hover:text-white">Privacy</Link></li>
                <li><Link href="/legal/cookies" className="hover:text-black dark:hover:text-white">Cookies</Link></li>
                <li><Link href="/legal/dmca" className="hover:text-black dark:hover:text-white">DMCA</Link></li>
              </ul>
            </div>
          </div>
          <div className="mt-10 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 text-xs text-black/60 dark:text-white/60">
            <div>© {new Date().getFullYear()} Flemmo Music Global. All rights reserved.</div>
            <div className="space-y-1">
              <div>FMG Publishing is a division of Flemmo Music Global.</div>
              <div>FMGIHub is a platform & network of Flemmo Music Global.</div>
            </div>
          </div>
        </div>
      </footer>
    </main>
  );
}
