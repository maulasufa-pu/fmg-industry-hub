// src/app/admin/projects/[id]/page.tsx
"use client";

export const dynamic = "force-dynamic";

import React from "react";

// The layout now handles all the logic and rendering
// This page component just provides additional content if needed
export default function AdminProjectDetailPage(): React.JSX.Element {
  return (
    <>
      {/* Additional page-specific content can go here */}
      {/* The layout handles all the main functionality */}
    </>
  );
}
